apiVersion: v1
kind: Service
metadata:
  name: relatorio-service
  namespace: default
spec:
  type: NodePort
  selector:
    app: relatorio-service
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8080
    nodePort: 30086
