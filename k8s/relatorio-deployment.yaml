apiVersion: apps/v1
kind: Deployment
metadata:
  name: relatorio-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: relatorio-service
  template:
    metadata:
      labels:
        app: relatorio-service
    spec:
      containers:
      - name: relatorio-service
        image: joaodainese/relatorio-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
          name: http
        envFrom:
          - configMapRef:
              name: relatorio-config
          - secretRef:
              name: relatorio-secret
        resources:
          requests:
            cpu: "50m"
            memory: "256Mi"
            ephemeral-storage: "1Gi"
          limits:
            cpu: "500m"
            memory: "512Mi"
            ephemeral-storage: "2Gi"
        livenessProbe:
          httpGet:
            path: /health/live
            port: 8080
          initialDelaySeconds: 60
          periodSeconds: 30
          timeoutSeconds: 10
          failureThreshold: 5
        readinessProbe:
          httpGet:
            path: /health/ready
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 15
          timeoutSeconds: 10
          failureThreshold: 5
        startupProbe:
          httpGet:
            path: /health/startup
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 15
          timeoutSeconds: 10
          failureThreshold: 20
